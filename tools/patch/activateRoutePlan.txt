    function activateRoutePlan(id){currentPlan=id;plans=planData[id];dayStats=routeStatsByPlan[id]||[];const tracks=roadTracksByPlan[id]||[];dailyRoutes=plans.map(p=>[places[p[3]],...(p[16]||[]).map(name=>places[name]),places[p[4]]]);gcjTracks=dailyRoutes.map((route,i)=>(tracks[i]?decodeTrack(tracks[i]):route).map(point=>wgs2gcj(point[0],point[1])));gcjAnchors=dailyRoutes.map(route=>route.map(point=>wgs2gcj(point[0],point[1])));selected=0;if(routeMap&&dayLayer)routeMap.removeLayer(dayLayer);dayLayer=null;setupOverviewMap();setupRouteMap();renderDays();render();const totals=planTotals[id],hasFlight=plans.some(p=>p[7]==='飞机'),stats=document.querySelectorAll('.summary div');stats[0].innerHTML=`<b>${plans.length}</b> 天计划`;stats[1].innerHTML=`<b>≈${fmtKm(totals.km)}</b> km 自驾总里程`;stats[2].innerHTML=`<b>≈${Math.round(totals.min/60)}h</b> 纯驾车总时长`;stats[3].innerHTML=hasFlight?`<b>✈ ${fmtKm(totals.fly.km)}</b> km 飞机返程`:`<b>${plans.filter(p=>p[3]!==p[4]).length}</b> 次转场`;document.querySelector('.map-key b').textContent=`PLAN ${id.toUpperCase()} · ${plans.length} DAYS · ≈${fmtKm(totals.km)} KM`;document.querySelector('.map-key span').textContent=hasFlight?'橙色实线：自驾 · 紫色虚线：飞机返程':'橙线去程 · 蓝线自驾返程';document.querySelector('.map small').textContent=planInfo[id].outline;$('#trip-dates').textContent=`● ${planInfo[id].dates}　/　10 人自驾`;$('#trip-total-days').textContent=String(plans.length);const kmStat=$('#trip-total-km');if(kmStat)kmStat.textContent=`≈${fmtKm(totals.km)}`;$('#footer-dates').textContent=`太原出发 · ${planInfo[id].dates.replace('2026 · ','')}`}
