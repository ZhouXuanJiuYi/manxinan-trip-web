    function render(){const p=plans[selected],day=selected+1,sights=p[8].split('|'),s=dayStats[selected]||{};let cum=0;for(let i=0;i<=selected;i++){const d=dayStats[i];if(d&&d.m!=='fly')cum+=d.km}$('#nav-now').textContent=`当前：D${day} · ${p[4]}`;$('#weather-city').textContent=`${p[12]} · D${day}`;$('#stay-name').textContent=p[9];$('#eat-name').textContent=p[10];const detail=$('#detail');detail.style.setProperty('--wash',p[15]);detail.classList.remove('swap');void detail.offsetWidth;detail.innerHTML=`<div class="number">${String(day).padStart(2,'0')}</div><div><p class="date">${p[0]} · ${p[1]} <span>${p[7]}</span></p><h3>${p[2]}</h3><div class="cities"><b>${p[3]}</b><i></i><b>${p[4]}</b></div><p class="note">${p[11]}</p></div><div class="data">${s.km>0?`<div class="hl"><span>${s.m==='fly'?'直飞时长':'纯驾车约'}</span><b>${fmtDur(s.min)}</b></div><div class="hl"><span>${s.m==='fly'?'空中距离':'今日里程'}</span><b>${fmtKm(s.km)}<i> km</i></b></div>`:`<div class="hl"><span>今日节奏</span><b>游玩日</b></div><div class="hl"><span>今日里程</span><b>市内短驳</b></div>`}<div><span>自驾累计</span><b>≈${fmtKm(cum)} / ${fmtKm(planTotals[currentPlan].km)} km</b></div><div><span>行程进度</span><b>D${day} · ${Math.round(day/plans.length*100)}%</b></div></div><div class="sights"><span>今日看什么</span>${sights.map(x=>`<b>✦ ${x}</b>`).join('')}</div><div class="detail-footer"><span>住：<b>${p[9]}</b></span><span>吃：<b>${p[10]}</b></span><button onclick="document.querySelector('#weather').scrollIntoView()">查看天气 ↗</button></div>`;detail.classList.add('swap');updateDays();$('#weather-card').innerHTML='<div class="card-top"><span>LIVE FORECAST</span><span>需更新</span></div><div class="weather-empty"><b>☁</b><span>点击“更新天气”<br>生成当前目的地的出行建议</span></div>';$('#weather-error').textContent='';updateRouteMap()}
